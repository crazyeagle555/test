---
- name: Configure PostgreSQL server
  hosts: 127.0.0.1
  connection: local
  become: true
  roles:
    - { role: ANXS.postgresql }
  vars_files:
    - "{{inventory_dir}}/postgresql.yml"

- name: Configure nginx server
  hosts: 127.0.0.1
  connection: local
  become: true
  roles:
    - { role: nginxinc.nginx }
  vars_files:
    - "{{inventory_dir}}/nginx.yml"
